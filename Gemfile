source 'https://rubygems.org'

gem 'durable_decorator', '~> 0.2.0'

group :development, :test do
  spree_branch = 'master'
  gem 'spree',                  github: 'spree/spree', branch: spree_branch
  gem 'spree_auth_devise',      github: 'spree/spree_auth_devise', branch: spree_branch
end

gemspec
